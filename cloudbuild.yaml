# Google Cloud Build Configuration
# Automates building and deploying to Cloud Run
#
# This project uses Artifact Registry (Container Registry / gcr.io is deprecated).
# Setup:
# 1) Create an Artifact Registry repo (example: akuriru-repo in asia-northeast1)
# 2) Create a Cloud Build trigger connected to your GitHub repo
# 3) On every push, Cloud Build will build+push+deploy automatically

steps:
  # Step 1: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/akuriru-repo/akuriru-stand:$COMMIT_SHA'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/akuriru-repo/akuriru-stand:latest'
      - '.'
    id: 'build-image'

  # Step 2: Push Docker image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/akuriru-repo/akuriru-stand:$COMMIT_SHA'
    id: 'push-image'

  # Step 3: Push latest tag
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/akuriru-repo/akuriru-stand:latest'
    id: 'push-latest'

  # Step 4: Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'akuriru-stand'
      - '--image=asia-northeast1-docker.pkg.dev/$PROJECT_ID/akuriru-repo/akuriru-stand:$COMMIT_SHA'
      - '--region=asia-northeast1'
      - '--platform=managed'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=10'
      - '--timeout=60s'
      - '--set-env-vars=APP_ENV=gcp,USE_CLOUD_STORAGE=true,GCP_PROJECT_ID=$PROJECT_ID,GCP_REGION=asia-northeast1'
      - '--add-cloudsql-instances=$_CLOUD_SQL_CONNECTION_NAME'
      - '--set-secrets=DB_PASS=DB_PASSWORD:latest,DB_USER=DB_USER:latest,DB_NAME=DB_NAME:latest,CLOUD_SQL_CONNECTION_NAME=CLOUD_SQL_CONNECTION_NAME:latest,GCS_BUCKET=GCS_BUCKET:latest'
    id: 'deploy-cloud-run'

# Images to store in Artifact Registry
images:
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/akuriru-repo/akuriru-stand:$COMMIT_SHA'
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/akuriru-repo/akuriru-stand:latest'

# Substitutions (can be overridden)
substitutions:
  _CLOUD_SQL_CONNECTION_NAME: 'your-project:asia-northeast1:acrylic-stand-db'
  _SERVICE_NAME: 'akuriru-stand'
  _REGION: 'asia-northeast1'

# Options
options:
  machineType: 'E2_MEDIUM'
  logging: CLOUD_LOGGING_ONLY

# Timeout for entire build
timeout: '1200s'

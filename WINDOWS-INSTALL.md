# 🪟 Windows 系统部署指南

你在 Windows 上部署 GCP，有两个选择：

---

## ✅ 推荐方案：使用 WSL2 (Windows Subsystem for Linux)

### 为什么推荐 WSL2？
- ✅ 完全兼容 Linux 工具
- ✅ 可以直接使用我们的脚本
- ✅ 性能好，集成度高
- ✅ 微软官方支持

---

## 🚀 步骤1: 安装 WSL2

### 1.1 打开 PowerShell（管理员权限）

**按键**: `Win + X` → 选择 "Windows PowerShell (管理员)" 或 "终端 (管理员)"

### 1.2 安装 WSL2

在 PowerShell 中运行：

```powershell
wsl --install
```

这个命令会：
- ✅ 启用 WSL
- ✅ 安装 Ubuntu（默认）
- ✅ 设置 WSL2

**需要时间**: 5-10分钟

### 1.3 重启电脑

```powershell
Restart-Computer
```

或者手动重启。

### 1.4 首次启动 WSL

重启后，打开 "Ubuntu" 应用（从开始菜单）

会要求你：
1. 创建用户名
2. 设置密码

**重要**: 记住这个密码！后面会用到。

---

## 🚀 步骤2: 在 WSL 中安装工具

### 2.1 更新系统

在 Ubuntu 终端中运行：

```bash
sudo apt update && sudo apt upgrade -y
```

输入你刚才设置的密码。

### 2.2 克隆你的项目

```bash
# 安装 git
sudo apt install -y git

# 克隆项目
cd ~
git clone https://github.com/Unlce/akuriru-stand.git
cd akuriru-stand
```

### 2.3 运行安装脚本

```bash
# 给脚本执行权限
chmod +x install-tools.sh

# 运行安装
sudo ./install-tools.sh
```

---

## 🚀 步骤3: 认证 GCP

安装完成后：

```bash
# 登录 GCP
gcloud auth login

# 设置项目
gcloud config set project acrylicstand
```

---

## 🚀 步骤4: 部署

```bash
# 设置成本保护
./setup-cost-protection.sh

# 部署
./deploy-to-gcp.sh
```

---

## ❓ 如果你已经有 WSL

如果你已经安装了 WSL，直接：

1. 打开 Ubuntu (或你的 WSL 发行版)
2. 进入项目目录
3. 运行安装脚本

---

## 🔄 替代方案：使用 Google Cloud Shell

如果你不想安装 WSL，可以使用 Google Cloud Shell（云端终端）：

### 优点
- ✅ 无需安装任何东西
- ✅ gcloud 已预装
- ✅ Docker 已预装
- ✅ 完全免费

### 步骤

1. **访问**: https://console.cloud.google.com/
2. **点击右上角的 Cloud Shell 图标** (>_ 图标)
3. **克隆项目**:
   ```bash
   git clone https://github.com/Unlce/akuriru-stand.git
   cd akuriru-stand
   ```
4. **设置项目**:
   ```bash
   gcloud config set project acrylicstand
   ```
5. **部署**:
   ```bash
   ./setup-cost-protection.sh
   ./deploy-to-gcp.sh
   ```

---

## 💡 我的推荐

**对于新手**: 使用 **Google Cloud Shell** 最简单！
- 不需要安装任何东西
- 浏览器就能用
- 5分钟就能开始部署

**对于长期使用**: 安装 **WSL2**
- 可以在本地开发
- 更好的集成体验

---

## 📞 下一步

请告诉我你想用哪种方式：

1. **WSL2** - 我会详细指导安装
2. **Cloud Shell** - 我会给你具体步骤

选择哪个？
